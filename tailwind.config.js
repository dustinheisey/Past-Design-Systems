/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/**/*.js'],
    darkMode: ['class', '[data-mode="dark"]'],
    theme: {
        extend: {},
    },
    plugins: [],
};