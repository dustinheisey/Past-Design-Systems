<div class="region prose stack align-start text-start gap-xs">
    <p class="overline">{{props.content.overline}}</p>
    <h1>{{props.content.headline}}</h1>
    <p>
        {{props.content.body}}
    </p>
    <a href="{{props.content.btn.href}}" class="btn">{{props.content.btn.label}}</a>
</div>