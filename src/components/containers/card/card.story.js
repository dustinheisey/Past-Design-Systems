import { card, cardGrid, cardImage, cardImageGrid } from "./card.js";

export default {
  title: "Components/Containers/Card",
};

export const Card = () => card;
export const CardGrid = () => cardGrid;
export const CardImage = () => cardImage;
export const CardImageGrid = () => cardImageGrid;
