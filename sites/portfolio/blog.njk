---
layout: base
eleventyNavigation:
key: Blog
order: 5
title: Accessibility & Sustainability Tips for Businesses & Nonprofits | Blog | Dustin Heisey
description: Explore my blog for insightful articles on user-centered, accessible, and environmentally sustainable web design practices. Stay updated with the latest trends and tips for eco-conscious businesses and nonprofits.
---

<div class="region cover cover-nav">
  <div class="principle">
    <div class="reverse sidebar sidebar-split gap-m">
      <div class="frame square">
        {% image content.blog.img.src, content.blog.img.alt %}
      </div>
      <div class="prose fixed">
        <p class="overline">{{content.blog.overline}}</p>
        <h1 class="page-headline">{{content.blog.headline}}</h1>
        <p>{{content.blog.body}}</p>
      </div>
    </div>
  </div>
</div>

<div class="region stack gap-l">
  <h2 class="region-headline text-center center-intrinsic">Recent Posts</h2>
  <div class="reel no-bar">
    {% for category in collections.categories %} {% set categoryUrl
    %}/categories/{{ category.data.label | slugify }}/{% endset %}
    <a href="{{ categoryUrl | url }}" class="badge badge-l badge-{{category.data.theme}}">{{ category.data.label}}</a>
    {% endfor %}
  </div>
  <div class="masonry col-3 gap-m">
    {% for post in collections.post | reverse %} {% set postCategory =
    collections.categories | getCategory(post.data.tags) %}
    <article class="prose card card-link theme-{{postCategory.data.theme}}-container">
      <a href="{{ post.url | url }}">
        <div class="cluster align-center gap-2xs">
          <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
          <div class="badge badge-primary">{{post.data.tags | tagsOnly}}</div>
        </div>
        <h2 class="section-headline">{{ post.data.title }}</h2>
        <p>{{ post.data.description }}</p>
      </a>
    </article>
    {% endfor %}
  </div>
</div>

<div class="region bg-pattern theme-primary">
  <div class="prose">
    <h2 class="region-headline">{{content.cta.headline}}</h2>
    <p>{{content.cta.body}}</p>
    <a href="/contact" class="btn">Start a Project</a>
  </div>
</div>